const express = require("express");
const router = express.Router();
const PastElection = require("../models/pastElection.model");

// Define routes for fetching past elections
// ...

module.exports = router;
