const express = require("express");
const router = express.Router();
const PastElection = require("../model/pastElection");

// Define routes for fetching past elections
// ...

module.exports = router;
